---
- name: Create configuration directory
  file:
    path: '{{ home }}/.ssh'
    state: directory

- name: Copy configuration files
  copy:
    src: './files/'
    dest: '{{ home }}/.ssh/'
    backup: '{{ create_backups }}'
